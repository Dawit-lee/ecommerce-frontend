# 프로덕션 빌드용 (Vercel)
# VITE_BACK_END_URL 를 비워두면 api/이미지 요청이 상대경로(/api, /images)로 나가고,
# vercel.json 의 rewrites 가 이를 백엔드(EB)로 프록시한다.
# (소스에서 `|| ""` 폴백 처리되어 미설정이어도 동일하게 동작)
VITE_BACK_END_URL=
VITE_STRIPE_PUBLISHABLE_KEY=pk_test_placeholder
